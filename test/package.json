{
	"name": "oracle-json",
	"description": "call oracle stored procedures using json input and output params",

	"version": "0.1.1",

	"dependencies": {
		"express":"3.4.x"
	},
	"devDependencies": {
	},
	"engines": {
		"node": "~0.10"
	}
}